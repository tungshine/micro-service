#进入页面http://localhost:7080/hystrix.stream
#监控http://localhost:7077/hystrix.stream
spring.application.name=hystrix-dashboard
server.port=7080

management.server.port=6666
# eureka configuration
eureka.client.serviceUrl.defaultZone=http://localhost:7070/eureka/
#eureka.instance.metadata-map.management.port=${management.server.port}

# spring boot admin configuration
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

# turbine configuration
#turbine.combine-host-port=true
#turbine.aggregator.cluster-config=hystrix
#turbine.app-config=service-admin,hystrix-dashboard, service-A
#turbine.cluster-name-expression="hystrix"
#turbine.stream.port=6666